# Exploration notes

Populated by the Warehouse BDD Explorer agent (`.github/agents/bdd-warehouse-explorer.agent.md`)
each time it's run. It is forbidden from reading this repo's source, so everything below
comes only from clicking around the running app - review it before trusting any of it.

Nothing has been run yet. Invoke the agent (see the repo README) to populate this file and
`src/Tests.UI/Features/Discovered/*.feature`.

